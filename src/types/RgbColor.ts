/**
 * An RGB colour used in diff output images.
 */
export type RgbColor = {
    /** Red channel (0–255). */
    r: number;
    /** Green channel (0–255). */
    g: number;
    /** Blue channel (0–255). */
    b: number;
};
