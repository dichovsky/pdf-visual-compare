import { ComparePdfError } from './ComparePdfError.js';

/**
 * Thrown when PDF rendering fails before visual comparison can start.
 */
export class ComparePdfRenderingError extends ComparePdfError {}
